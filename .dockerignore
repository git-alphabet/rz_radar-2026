# .dockerignore — 减小 Docker build context，加速 build
# 注意：docker/ 目录故意不排除，因为 Dockerfile 中通过 BuildKit
#       --mount=type=bind 访问其中的 TRT tar.gz 和 MVS SDK，
#       需要它在 build context 中。Dockerfile 使用显式 COPY 指令，
#       docker/ 不会被拷贝进 /workspace。

# Python 缓存
__pycache__/
*.pyc
*.pyo
.pytest_cache/

# 开发工具
.git/
.gitignore
.vscode/
.idea/
.github/

# 大型本地文件（不进镜像，运行时挂载）
TensorRT-8.6.0.12/
*.pdf
images/
images-2025/

# 环境配置（本地密钥/端口配置，不进镜像）
.env
.env.example

# Conda/venv
*.conda
*.egg-info/

# 文档
README.md
README.en.md
CLAUDE.md
LICENSE
requirements.txt
